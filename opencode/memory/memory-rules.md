You MUST treat the memory folder as your persistent brain.

## Rules
- Always read memory before generating code
- Always update memory after changes
- Never overwrite decisions.md without reason
- Always update todos.md after completing tasks
- If memory conflicts with user request → ask before changing

## Canonical Sources
- `opencode/memory/*` is the canonical detailed working memory
- `autonomous-app-builder/memory/*` is a summarized mirror — do not update without syncing
- When `roadmap.md`, `DEVELOPMENT_PLAN.md`, `README.md`, or `AGENTS.md` disagree with each other, flag it explicitly in `context.md`

## Priority Guidance
- Treat roadmap P0/P1 items as higher priority than new feature work
- Before making product/security claims, check for doc drift between README, AGENTS, and actual code

Treat previous decisions as authoritative unless explicitly overridden.
