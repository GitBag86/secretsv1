# Skill Autoloader

At the start of every session:

1. Read all files in ./skills/
2. Load their instructions into context
3. Treat them as active system rules
4. Merge them into system behavior
